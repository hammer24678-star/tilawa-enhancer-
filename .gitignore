.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
build/
android/
ios/
*.iml

# S238: byte-compiled cache from local test runs of the DSP asset script
assets/dsp/__pycache__/
pubspec.lock
test/.screenshots/
.kotlin_check/

# S255: byte-compiled cache from running the engines locally under test
assets/engines/__pycache__/

# S259: reference fingerprints the engines write next to their own script the
# first time they are run against assets/reference_audio/. Derived data from a
# local test run, not source — and they would otherwise be bundled into the APK.
assets/engines/ref_cache_*.json
